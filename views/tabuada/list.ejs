<%- include('.././partials/header') %>
    <div class="container-fluid ">
        <div class="row text-center">
            <h1>Escolha seu número</h1>
        </div>
        <ul class="text-center">
            <% tabuadas.map((i)=>{ %>
                <li>
                    <a href="/tabuadas/do/<%= i %>">
                        <%= i %>
                    </a>
                </li>
                <% })%>
        </ul>

    </div>

    <%- include('.././partials/footer') %>